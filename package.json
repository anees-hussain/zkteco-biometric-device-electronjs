{
  "name": "biometric-attendance-app",
  "version": "1.0.0",
  "description": "A desktop application to fetch attendance data from a biometric device and export it to Excel.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . biometric-attendance-app --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "dependencies": {
    "electron": "^25.0.0",
    "node-zklib": "^1.0.0",
    "xlsx": "^0.17.0"
  },
  "devDependencies": {
    "electron-packager": "^15.0.0"
  }
}